#include <stdio.h>

int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    if(a%2==0)
        printf("even\n");
    else
        printf("odd\n");   
    if(b%2==0)
        printf("even");
    else
        printf("odd");
    // 여기에 코드를 작성해주세요.
    return 0;
}